// arr.join(glue) - Объединяет массив по символу переданному в метод

const arr = ['apple', 'banana', 'grapefruit', 'orange', 'potato', 'cucumber', 'melon', 'cherry'];

console.log(
    arr.join(', ')
)

console.log(
    arr.join('')
)
